## interceptor-vfs

Interceptor-vfs is a library which implements a virtual filesystem and
intercepts libC IO calls to use the new file system.

It can be used fully in user-space without any required privilege,
therefore suitable for very secure and restricted containerized setups.

